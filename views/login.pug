extends layouts/main

block content
    .login
        form.login__form(action="/login", method="POST")
            input.login__input(type="text", name="email", placeholder="Email", required=true)
            input.login__input(type="password", name="password", placeholder="Password", required=true)
            input.login__btn(type="submit", value="Submit")